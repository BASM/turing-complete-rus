kind = architecture
title = "Генератор Случайных Чисел"
tests = 64
size = [-256, -256, 255, 255]
background = 4
arch_input_word_size = 16
dialogue = [
  (alien_centered, "Нам всем нравится, как Робот двигается на танцполе. Вот почему мы хотим, чтобы он возглавил нашу танцевальную команду.

Единственная проблема придумывать оригинальные танцевальные последовательности. Как, вы спрашиваете, сделать креативность из детерминированной логики?

Ответ – генераторы псевдослучайных чисел."),
  (level_centered, `В этом уровне у вас только одно единственное значение на входе, это seed (начальное значение). Чтобы сделать случайное число, seed должен пройти следующие шаги.

Помните, "lsl"(Logic Shift Left) это логический сдвиг влево, а "lsr"(Logic Shift Right) логический сдвиг вправо.

[b]16-битный xorshift ПСП:[/b][code][color=#87a8c8]
temp1  = seed  xor (seed  lsr 7)
temp2  = temp1 xor (temp1 lsl 9)
result = temp2 xor (temp2 lsr 8)
[/color][/code]

После чего, выведите "result"(результат) и затем используйте его в качестве seed(начальное значение) для следующей итерации.
`)]
