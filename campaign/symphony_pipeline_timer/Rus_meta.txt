kind = sequential
title = "Таймер конвейера"
size = [-48, -48, 49, 49]
background = 6
save_solution_as_custom = "Symphony/Allegro/timer"
input_labels = [["Старт"]]
output_labels = ["В процессе"]
add_components = [com_probe_memory_word: -1]
dialogue = [
    (alien, `Вы нужны нам для работы над созданием новой версии Symphony, которую мы называем Allegro, которая выполняет те же инструкции, что и Symphony, но намного быстрее, чтобы мы могли играть в игры на ней.

Также было бы неплохо, если бы компьютер был энергоэффективным, но в основном нам нужно, чтобы он был достаточно быстрым для запуска наших игр. По этой причине все решения уровней с этого момента должны иметь задержку не более 60.`),
    (alien, `Чтобы сделать Allegro достаточно быстрым, вам нужно реализовать несколько техник оптимизации процессора.

Конечная цель построить конвейерную, суперскалярную, с кешированием памяти и спекулятивным выполнением версию Symphony.`),
    (info, `Создайте таймер, который позже будет использоваться в конвейере.

Когда вход [ON], таймер активируется. Когда таймер активирован, выход должен быть таким: [OFF][ON][ON]. То есть [OFF] на первом такте и [ON] на двух последующих тактах.
Если таймер находится в середине последовательности, его нельзя активировать повторно. Другими словами, игнорируйте вход, если выход [ON].
`)
]


