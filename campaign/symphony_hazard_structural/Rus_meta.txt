kind = sequential
title = "Структурный конфликт"
size = [-48, -48, 49, 49]
background = 6
input_labels = ["Instruction", "RAM ready"]
output_labels = ["Execute"]
copy_solution_to_factory = "Symphony/Allegro/hazard/haz_struct"
dialogue = [
  (alien, `Пора убрать компонент Быстрой RAM из вашей схемы, его характеристики по количеству вентилей слишком высоки.

Мы заменим его на Latency RAM(RAM с задержкой). Эта RAM дешева как по количеству вентилей, так и по задержке, но ей требуется несколько тактов для завершения.`),
  (alien, `Как и в уровне с конфликтом данных, цель этого уровня проверить, можем ли мы безопасно выполнять инструкции. 

Если входная инструкция является RAM инструкцией, мы считаем, что RAM занята, пока не увидим сигнал готовности RAM.

Пока RAM занята записью, мы можем выполнять только АЛУ(ALU) и IO(ввод-вывод) инструкции.
Пока RAM занята чтением, мы не можем выполнять ничего. 

Мы бездействуем, ожидая завершения чтения, чтобы упростить архитектуру пока что. Инструкции загрузки перезаписывают регистр, поэтому нам нужно убедиться, что мы не используем этот регистр до его загрузки. Также, результат загрузки требует сохранения в регистр, поэтому нам нужно убедиться, что один из конвейеров свободен.`),
]
