kind = architecture
title = "Условные Переходы"
tests = 42
size = [-256, -256, 255, 255]
no_score = true
dialogue = [
  (info, "Читайте вход до тех пор, пока не считаете число 37.

Затем выведите сколько вы сделали чтений из входа."),
  (info, `Для прохождения уровня вам понадобится использовать инструкцию jump(переход).

Jump(переход) это инструкция, которая перезаписывает счетчик.

Перезапись счетчика означает, что мы переходим(jump) к инструкции в другом месте нашей программы, вместо того чтобы просто переходить к следующей.`),
  (info, `Примечание: Если вы используете явное число для места перехода, вам придется обновлять его каждый раз, когда вы добавляете или удаляете код выше места назначения прыжка.

Вместо этого вы можете использовать метку(label) для обозначения места в программе. Эта метка хранит в себе текущий адрес, например:

my_label:     ; Это метка
imm my_label  ; Загрузить смещение метки в r0
  `),
]
default_assembly = `
imm 5 ; Записать 5 в r0
jmp   ; Перейти на 5 опкод

; <- Нажимте Ассемблер чтобы увидеть другие опкоды условного перехода

my_label:    ; Это метка
imm my_label ; Вы можете загрузить значение так

`
